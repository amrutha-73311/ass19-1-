#include<stdio.h>
#include<conio.h>
int main()
{
int n,i;
unsigned long fact=1;
printf("enter an integer:");
scanf("%d:,&n);
if(n<0)
printf("error! factorial of a negative number doesnt't exist:");
else
{
for(i=2;i<n;i++);
{
fact*=i;
}
printf("factorial of %d=%lf,n,fact);
}
return0;
}
